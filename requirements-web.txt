# requirements-web.txt
# LifeSaver Web UI - FastAPI + WebSocket layer on top of the core scanner.
# Install IN ADDITION to requirements.txt:
#   pip install -r requirements.txt
#   pip install -r requirements-web.txt

fastapi
uvicorn[standard]
websockets
